# Agentic-Demo-CSVIngestion

CSV ingestion pipeline with two implementations: **explicit graph** (fixed flow) and **single ReAct agent** (full autonomy over tools). See [OVERVIEW.md](OVERVIEW.md) for architecture and both implementations.